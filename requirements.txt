# MkDocs + Material theme dependencies
# Install with: pip install -r requirements.txt

mkdocs>=1.5.0
mkdocs-material>=9.5.0

# Plugins
mkdocs-glightbox>=0.3.6         # Image lightbox
# mike>=2.0.0                   # Versioned docs — install separately when running: mike deploy <version>

# Show last-modified date per page (requires full git history — fetch-depth: 0 in CI)
mkdocs-git-revision-date-localized-plugin>=1.2.0

# ============================================================
# Quick start:
#   pip install -r requirements.txt
#   mkdocs serve          # Preview at http://127.0.0.1:8000
#   mkdocs build          # Build static site → site/
#   mkdocs gh-deploy      # Deploy to GitHub Pages (branch: gh-pages)
#
# Versioned deploy (after initializing mike):
#   mike deploy --push --update-aliases 1.0 latest
#   mike set-default --push latest
# ============================================================
